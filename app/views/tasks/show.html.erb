<%= @task.completed ? "TICK" : "-X-" %><h1><%= @task.title %></h1>

<p><%= @task.details %></p>


<%= link_to 'All', tasks_path %>
<br>
<%= link_to 'Edit', edit_task_path(@task) %>
<%= link_to 'Delete', task_path(@task),
method: :delete, data: {confirm: "Are you sure?"} %>
